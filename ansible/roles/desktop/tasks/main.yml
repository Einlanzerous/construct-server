---
# --- 1. LANGUAGES ---
- name: Install TypeScript globally
  npm:
    name: typescript
    global: true
    state: present

# --- 2. DESKTOP APPS ---
- name: Install Snapd
  apt:
    name: snapd
    state: present
